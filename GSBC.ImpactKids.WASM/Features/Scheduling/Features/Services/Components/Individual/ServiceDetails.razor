@inherits DetailsComponent<Service, CreateServiceRequest, UpdateServiceRequest>
@inject IRefreshableStore<ServiceType> ServiceTypesStore
@inject IRefreshableStore<SchoolTerm>  SchoolTermsStore

<MudPaper Elevation="0" Class="d-flex flex-column flex-grow-1">
    <MudOverlay Visible="@(Entity.IsLoading)"
                DarkBackground="true"
                Absolute="true">
        <MudProgressCircular Indeterminate/>
    </MudOverlay>
    @if (Entity.HasError)
    {
        <MudAlert Severity="Severity.Error" Class="mb-4">
            @(Entity.Error ?? "An Unexpected error occured")
        </MudAlert>
    }
    <MudTextFieldCreateOrUpdate
        @bind-Create="@CreateRequest.Name"
        Read="@UpdateRequest.Name.Value"
        @bind-Update="@UpdateRequest.Name.Value"
        State="State"

        Class="mb-4"
        Label="Name"
        Variant="Variant.Filled"
        Immediate/>
    <MudRequiredDatePickerCreateOrUpdate
        @bind-Create="@CreateRequest.LocalDate"
        Read="@UpdateRequest.LocalDate.Value"
        @bind-Update="@UpdateRequest.LocalDate.Value"
        State="State"

        Class="mb-4"
        Label="Date"
        Variant="Variant.Filled"

        DateFormat="dd/MM/yyyy"
        Editable/>

    <div class="relative mb-4">
        <MudOverlay Visible="@(SchoolTermsStore.GetState().Entities.IsLoading)"
                    DarkBackground
                    Absolute>
            <MudProgressCircular Indeterminate/>
        </MudOverlay>
        <MudSelectCreateOrUpdate
            @bind-Create="@CreateRequest.SchoolTermId"
            Read="@UpdateRequest.SchoolTermId.Value"
            @bind-Update="@UpdateRequest.SchoolTermId.Value"
            State="State"

            Label="School Term"
            Variant="Variant.Filled"

            Clearable>
            @foreach (SchoolTerm schoolTerm in GetSchoolTermsForDropdown())
            {
                <MudSelectItem T="Guid?"
                               Value="@schoolTerm.Id">
                    @(schoolTerm.Name)
                </MudSelectItem>
            }
        </MudSelectCreateOrUpdate>
    </div>

    <div class="relative mb-4">
        <MudOverlay Visible="@(ServiceTypesStore.GetState().Entities.IsLoading)"
                    DarkBackground
                    Absolute>
            <MudProgressCircular Indeterminate/>
        </MudOverlay>
        <MudSelectCreateOrUpdate
            @bind-Create="@CreateRequest.ServiceTypeId"
            Read="@UpdateRequest.ServiceTypeId.Value"
            @bind-Update="@UpdateRequest.ServiceTypeId.Value"
            State="State"

            Label="Service Type"
            Variant="Variant.Filled"

            Clearable>
            @if (ServiceTypesStore.GetState().Entities.HasData)
            {
                foreach (ServiceType serviceType in ServiceTypesStore.GetState().Entities.Data!)
                {
                    <MudSelectItem T="Guid?"
                                   Value="@serviceType.Id"
                                   Style="@($"color: {serviceType.Color ?? ServiceType.ColorBackup}")">
                        @serviceType.Label
                    </MudSelectItem>
                }
            }
        </MudSelectCreateOrUpdate>
    </div>
</MudPaper>