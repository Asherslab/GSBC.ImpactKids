@inject IRefreshableStore<MemoryVerse> MemoryVersesStore

<MudLink Underline="Underline.None"
         Href="@Href">
    @if (ShowReference && !ShowOriginalText && !ShowVerseText)
    {
        <MudPaper Elevation="Elevation"
                  Class="@ReferenceOnlyClass">
            <MudText Typo="Typo.body1" Class="ma-3">
                @(_memoryVerse.Data?.ReferenceName ?? "Memory Verse")
            </MudText>
        </MudPaper>
    }
    else
    {
        <MudCard Elevation="Elevation" Class="relative">
            <MudCardHeader>
                <CardHeaderContent>
                    <MudPaper Elevation="0" Class="d-flex justify-space-between flex-grow-1">
                        <MudText Typo="Typo.h6" Align="Align.Start">
                            @(_memoryVerse.Data?.ReferenceName ?? "Memory Verse")
                        </MudText>
                    </MudPaper>
                </CardHeaderContent>
            </MudCardHeader>
            <MudCardContent>
                <MudOverlay Visible="@(_memoryVerse.IsLoading)" DarkBackground="true" Absolute="true">
                    <MudProgressCircular Indeterminate/>
                </MudOverlay>
                @if (ShowVerseText)
                {
                    <MudText>
                        @(_memoryVerse.Data?.Verse ?? "Verse....")
                    </MudText>
                }
            </MudCardContent>
        </MudCard>
    }
</MudLink>