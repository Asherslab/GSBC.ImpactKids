@inherits DisplayComponent<AttendanceItemType>

<MudPaper Elevation="6" Class="relative">
    <MudOverlay Visible="@(Entity.IsLoading)" DarkBackground="true" Absolute="true">
        <MudProgressCircular Indeterminate/>
    </MudOverlay>
    <MudLink Underline="Underline.None" Href="@Href">
        <MudPaper Elevation="0" Class="@Class">
            <MudAvatar Rounded Color="@AvatarColor" Class="ma-3">
                <MudIcon Color="Color.Surface" Icon="@AvatarDisplay"/>
            </MudAvatar>
            <MudPaper Elevation="0" Class="d-flex justify-center flex-column flex-grow-1">
                <MudText>
                    @DisplayText
                </MudText>
            </MudPaper>
            <MudPaper Elevation="0" Class="d-flex justify-center flex-column flex-grow-0 mr-5">
                @if (SuffixContent != null)
                {
                    @SuffixContent(Entity.Data)
                }
            </MudPaper>
        </MudPaper>
    </MudLink>
</MudPaper>