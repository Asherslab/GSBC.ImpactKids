---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "wasm-service"
  labels:
    app.kubernetes.io/name: "impact-kids-app"
    app.kubernetes.io/component: "wasm"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "impact-kids-app"
    app.kubernetes.io/component: "wasm"
    app.kubernetes.io/instance: "{{.Release.Name}}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: {{ .Values.parameters.wasm.port_http }}
      targetPort: {{ .Values.parameters.wasm.port_http }}
