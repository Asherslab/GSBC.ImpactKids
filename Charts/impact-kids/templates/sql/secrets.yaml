---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: "sql-secrets"
  labels:
    app.kubernetes.io/name: "impact-kids-app"
    app.kubernetes.io/component: "sql"
    app.kubernetes.io/instance: "{{.Release.Name}}"
data:
  MSSQL_SA_PASSWORD: {{ sqlPass | b64enc }}
type: "Opaque"
